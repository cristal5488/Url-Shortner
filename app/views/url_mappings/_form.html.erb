<%= form_with(model: url_mapping, local: true) do |form| %>
  <% if url_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(url_mapping.errors.count, "error") %> prohibited this url_mapping from being saved:</h2>

      <ul>
      <% url_mapping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :longurl %>
    <%= form.text_area :longurl, id: :url_mapping_longurl, rows: 1, cols:79 %>
  </div>

  <div class="field">

    <%= form.hidden_field :code, id: :url_mapping_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
